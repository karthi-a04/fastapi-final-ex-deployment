apiVersion: v1
kind: Service
metadata:
  name: fastapi-service-app2
  namespace: fastapi-app
spec:
  type: ClusterIP
  selector:
    app: fastapi
    service: app2
  ports:
    - port: 80
      targetPort: 80
