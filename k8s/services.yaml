apiVersion: v1
kind: Service
metadata:
  name: app1-service
  namespace: fastapi-app
spec:
  type: LoadBalancer
  selector:
    app: app1
  ports:
    - port: 80
      targetPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: app2-service
  namespace: fastapi-app
spec:
  type: LoadBalancer
  selector:
    app: app2
  ports:
    - port: 80
      targetPort: 80
